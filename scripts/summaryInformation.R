# General setup
library("dplyr")
library("ggplot2")

# Summary information

get_funding_summary_info <- function(dataset) {
  funding_sum <- list(
    num_districts = length(dataset)
    # 2. ____ = ____
    # 3. ____ = ____
    # 4. ____ = ____
    # 5. ____ = ____
  )
  funding_sum
}