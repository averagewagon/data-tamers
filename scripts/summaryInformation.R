# General setup
library("dplyr")
library("ggplot2")

# Summary information

get_funding_summary_info <- function(dataset) {
  funding_sum <- list()
  funding_sum$num_districts <- length(dataset)
  # 2. funding_sum$____ <- _____
  # 3. funding_sum$____ <- _____
  # 4. funding_sum$____ <- _____
  # 5. funding_sum$____ <- _____
  funding_sum
}